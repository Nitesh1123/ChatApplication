services:
  - type: web
    name: chatify-backend
    runtime: node
    buildCommand: npm run build
    startCommand: cd backend && node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
